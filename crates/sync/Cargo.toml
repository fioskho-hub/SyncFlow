[package]
name = "syncflow-sync"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
syncflow-core = { path = "../core" }
syncflow-storage = { path = "../storage" }
serde = { version = "1.0", features = ["derive"] }